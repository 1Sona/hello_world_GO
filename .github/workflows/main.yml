name: go version

on:
  workflow_dispatch:
    inputs:
      version:
        description: Select version  
        default: 1.19.1

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up go      
        uses: actions/setup-go@v3
        with:
          go-version: ${{inputs.version}}
        
      - run: go run hello.go
      - run: go run main_test.go
